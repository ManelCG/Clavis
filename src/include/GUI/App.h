#pragma once

#include <gtkmm.h>
#include <GUI/MainWindow.h>

namespace Clavis::GUI {
    class App {
        public:
            App();
            void Run(int argc, char* argv[]);

        protected:
        private:
    };
}